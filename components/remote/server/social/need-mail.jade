doctype html
html
    head
        title Login
        meta(name='viewport', content='width=device-width, initial-scale=1.0')
        link(rel='stylesheet', href='/layouts/core.min.css?#{cacheBreaker}')

    body
        div.page
            div.container
                div.row
                    div.col-sm-6
                        div.page-header
                            h1 Complete Register
                        div#register

        script(type='text/template', id='tmpl-register')
            form
                div.alerts
                    | <% _.each(errors, function(err) { %>
                    div.alert.alert-danger.alert-dismissable
                        button.close(type='button', data-dismiss='alert') &times;
                        | <%- err %>
                    | <% }); %>
                div.form-role(class!='<%- errfor.email ? "has-error" : "" %>')
                    label Enter Your Email:
                    input.form-control(type='text', name='email', value!='<%= email %>')
                    span.help-block <%- errfor.email %>
                div.form-role
                    button.btn.btn-primary.btn-register(type='button') Create My Account

        script(type='text/template', id='data-email') !{email}

        script(src='/layouts/core.min.js?#{cacheBreaker}')
        script(src='/register/need-mail.js?#{cacheBreaker}')




